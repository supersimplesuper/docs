{
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.2.10",
    "prettier": "3.4.1"
  },
  "scripts": {
    "format-check": "prettier --write --ignore-unknown --check .",
    "format": "prettier --write --ignore-unknown .",
    "prepare": "husky"
  },
  "lint-staged": {
    "**/*": "prettier --write --ignore-unknown"
  }
}
